SHELL=/bin/bash
TAG=v5
MANIFEST=./manifest
CONF=./conf
SCRIPTS=./scripts
TOOLS=./tools
DOCKERFILES=./Dockerfiles
NAME=database
NAMESPACE=database
LABELS_KEY=app
LABELS_VALUE=${NAME}
IMAGE_PULL_POLICY=IfNotPresent
IMAGE_PULL_POLICY2=Always
LOCAL_REGISTRY=punctual.reg.me/test
SCRIPTS_CM=${NAME}-scripts
CONF_CM=${NAME}-conf
BIN_CM=${NAME}-bin
ENV_CM=${NAME}-env
COMPILE_CM=${NAME}-compile
COMPILE=./compile
URL=punctual.db.me
PORT=8080
SCHEDULE=*/5 * * * *
DISCOVERY_NAME=${NAME}-headless
DISCOVERY_NAMESPACE=${NAMESPACE}
OBJECT=${NAME}
SERVICE_ACCOUNT=admin
ADMIN=admin
ADMIN_CONF_PATH=/etc/kubernetes/admin.conf
SVC1=${DISCOVERY_NAME}
SVC2=proxy

NAME0=init
IMAGE0=${LOCAL_REGISTRY}/${NAME0}:${TAG}

NAME1=redis0
IMAGE1=redis:5

NAME2=redis1
IMAGE2=${IMAGE1}

NAME3=cassandra
IMAGE3=${LOCAL_REGISTRY}/${NAME3}:${TAG}

NAME4=proxy
IMAGE4=${LOCAL_REGISTRY}/${NAME4}:${TAG}

NAME5=monitor
IMAGE5=${LOCAL_REGISTRY}/${NAME5}:${TAG}

NAME6=copier
IMAGE6=${LOCAL_REGISTRY}/${NAME6}:${TAG}

NAME7=sentinel
IMAGE7=${LOCAL_REGISTRY}/${NAME7}:${TAG}
NAME8=${NAME7}0
NAME9=${NAME7}1

NAME10=dev
IMAGE10=${LOCAL_REGISTRY}/${NAME10}:${TAG}
